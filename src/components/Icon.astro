---
interface Props {
  href: string;
  type: string;
}

const { href, type } = Astro.props;
---

<a href={href} target="_blank" class="ml-1 hover:animate-blurred-fade-in"><i class={`text-xl ${type}`}></i></a>
