---
interface Props {
  href: string;
  name: string;
}

const { name, href } = Astro.props;
---

<a
  href={href}
  target="_blank"
  class="relative flex justify-center items-center w-full border bg-[#0a0a0a00] text-white text-center py-3 my-3 px-4 cursor-pointer rounded-md max-sm:rounded-none"
>
  <slot />
  <span>{name}</span>
</a>
